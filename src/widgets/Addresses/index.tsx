import CheckAddress from '../../features/CheckAddress/ui';

const Addresses = () => {
    return (
        <div className="a">
            <CheckAddress />
        </div>
    );
};

export default Addresses;
